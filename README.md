# asset-recognition-classification-api
End-to-end CNN-based asset recognition and classification API. PyTorch model with FastAPI deployment, featuring data preprocessing, augmentation, and comprehensive evaluation metrics (accuracy, precision, recall, confusion matrix). Includes /predict endpoint for image uploads returning label + confidence scores.
